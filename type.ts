// Define a custom type
type Person = {
  name: string;
  age: number;
};

// Use the custom type
let user: Person = {
  name: "Alice",
  age: 30,
};

console.log(user.name); // Alice
console.log(user.age);  // 30
