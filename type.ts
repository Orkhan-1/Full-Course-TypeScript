// Define a custom type
type Person = {
  name: string;
  age: number;
};

// Use the custom type
let user: Person = {
  name: "Orkhan",
  age: 30,
};

console.log(user.name); // Orkhan
console.log(user.age);  // 30
